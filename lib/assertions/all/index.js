import { assert, refute } from './assert';
import { equal, notEqual } from './equal';
import { same, notSame } from './same';
import { match, doesNotMatch } from './match';
import fail from './fail';
import throws from './throws';

export default {
  assert,
  equal,
  throws,
  same,
  refute,
  notEqual,
  notSame,
  fail,
  match,
  doesNotMatch
};